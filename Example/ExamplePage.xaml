<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:Example" x:Class="Example.ExamplePage">
	<ContentPage.Content>

		<Grid>

			<Grid.RowDefinitions>
				
				<RowDefinition Height = "*" />
				<RowDefinition Height = "*" />
				<RowDefinition Height = "*" />
				<RowDefinition Height = "*" />
				<RowDefinition Height = "*" />

			</Grid.RowDefinitions>

			<Grid.ColumnDefinitions>

				<ColumnDefinition Width = "*" />
				<ColumnDefinition Width = "*" />

			</Grid.ColumnDefinitions>

			<Label x:Name="timeLabel" Grid.Row="1" Grid.Column="0" 
				Text= "0" Font= "10" />

			<Button x:Name="startButton" Grid.Row="2" Grid.Column="0" 
				Text= "Start" Clicked="start_Clicked" />

			<Button x:Name="stopButton" Grid.Row= "3" Grid.Column="0"
				Text= "Stop" Clicked ="stop_Clicked" />
			
			<Button x:Name="resetButton" Grid.Row="4" Grid.Column="0"
				Text= "Reset" Clicked="reset_Clicked" />

		</Grid>


	</ContentPage.Content>




<!--	<Label Text="Welcome to Xamarin Forms!" VerticalOptions="Center" HorizontalOptions="Center" />-->
</ContentPage>
