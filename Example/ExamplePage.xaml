<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:Example" x:Class="Example.ExamplePage">
	<ContentPage.Content>

		<Grid>

			<Grid.RowDefinitions>
				
				<RowDefinition Height = "*" />
				<RowDefinition Height = "*" />
				<RowDefinition Height = "*" />
				<RowDefinition Height = "*" />
				<RowDefinition Height = "*" />

			</Grid.RowDefinitions>
			 
			<Grid.ColumnDefinitions>

				<ColumnDefinition Width = "*" />
				<ColumnDefinition Width = "*" />

			</Grid.ColumnDefinitions>

			<Label x:Name= "lapLabel" Grid.Row="1" Grid.Column = "0" Grid.ColumnSpan="2"
				Text="Previous Lap: " Font= "30"/>
			
			<Label x:Name="timeLabel" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
				Text= "00.00.00.000" Font= "60" />

			<Button x:Name="startButton" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
				Text= "Start/Lap" Font= "80" Clicked="start_Clicked" />

			<Button x:Name="stopButton" Grid.Row= "4" Grid.Column="0" Grid.ColumnSpan="2"
				Text= "Stop" Font= "80" Clicked ="stop_Clicked" />
			
			<Button x:Name="resetButton" Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" 
				Text= "Reset" Font= "80" Clicked="reset_Clicked" />

		</Grid>


	</ContentPage.Content>




<!--	<Label Text="Welcome to Xamarin Forms!" VerticalOptions="Center" HorizontalOptions="Center" />-->
</ContentPage>
